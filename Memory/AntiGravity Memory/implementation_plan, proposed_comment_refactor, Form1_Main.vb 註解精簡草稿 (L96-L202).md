# Form1_Main.vb 註解精簡草稿 (L96-L202)

以下是我打算替換的註解內容。我將原本分散的「摘要」、「為什麼不用並行」、「效能特點」整合為更緊湊的區塊，並保留了您要求的發展歷程：

```vb
#Region "■ 04 Tab1: 資料夾統計 — 重構後程式碼 v5 (最終版) ==="
    ' ==============================================================
    ' 2026/04/04 by AntiGravity: 註解精簡與邏輯整合。
    '
    ' ── 1. 版本演進與技術對抗 ───────────────────────────────────
    '   原始版  循序 Await (STA 違規、結果不明顯快取)。
    '   v1~v3   BFS 初步嘗試、Task.WhenAll 並行優化。
    '           結論: PST 是單一檔案且 PR_CONTENT_COUNT 讀取受 COM overhead 主導，
    '           並不適合大規模 ThreadPool dispatch (單次 dispatch ~1ms)。
    '           v2 (並行) 比 v1 (循序) 慢 40~80ms，即是導因於此。
    '   v4      回歸 BFS Sequential + root 永遠展開直屬子資料夾 (Bug fix)。
    '           Cache Hit: 0.01s (最快，完全不觸發 Task.Run)。
    '   v5      (本版) 依照「單一職責原則」將核心 L2 拆解為 5 小步函數，確保擴充性。
    '
    ' ── 2. 三層式架構與資料流 (STA 安全) ─────────────────────────
    '   L1 (UI)   TreeView1_AfterSelect: 調度、防護序列 (_tab1SelectSeq)、更新 ListView。
    '   L2 (流程) ComputeFolderStatsAsync: BFS 控制器、管理快取剪枝、底向上彙總。
    '   L3 (資料) GetMailCount: 純 COM 讀取層 (PR_CONTENT_COUNT)，最小化呼叫。
    '   特性: 所有 COM 呼叫均在 UI 執行緒以策安全；耗時運算用 Task.Yield() 出讓主權。
    '
    ' ── 3. 快取及效能策略 ───────────────────────────────────────
    '   Key: Outlook.Folder 物件路徑 (FolderPath)。
    '   快取內容: 郵件數 (mailCountCache)、子資料夾數 (folderCountCache)、排序清單。
    '   剪枝規則:
    '     - Root   (BFS 起點) → 永遠展開直屬子。
    '     - 非 Root (命中快取) → 標記 IsFromCache 並停止向下遍歷。
    '   效能點: 首次點選進行整棵子樹預讀；後續點選幾乎瞬間 (0.01s) 完成記憶體累加。
    '
    ' ── 4. 定向開發紀錄 (僅供遷移參考) ───────────────────────────
    '   [取代] GetInfoForListview、GetFolderSizeLegacy (修正 STA 違規)。
    '   [保留] GetMailCountByMAPINew (Fallback 用)、GetSortedSubFolders。
    '   [注意] _tab1SelectSeq 需放置於全域，確保快速點選時 ListView 不會出現舊數據。
    ' ==============================================================
    ' ─────────────────────────────────────────────────────────────
    ' FolderBfsEntry: BFS 過程中每個資料夾節點的容器
    ' 貫穿 L2 的所有步驟 (BFS 展開 → L3 讀取 → 底部向上彙總 → 快取寫入 → 回傳清單)
    ' ─────────────────────────────────────────────────────────────
```

### 優化重點說明：
1. **去重**：原本在「版本摘要」和「為什麼不並行」重複提到的 PST I/O 競爭與 `Task.Run` overhead 現在統一整合在「技術對抗」小節。
2. **緊密度**：將「效能特點」併入「快取策略」，因為效能完全依賴快取命中。
3. **可讀性**：使用了明確的 L1/L2/L3 結構定義，方便日後維護。
4. **保留**：保留了 v1-v5 的發展脈絡與原本代碼內部的 `todo` 思考（例如：單執行緒 vs 並行的觀察）。

如果您認可這個版本，我再將其更新至 `Form1_Main.vb`。
