# F1 快速鍵切換側邊欄功能實作完成

我已經在 `Form1.vb` 中實作了 F1 快速鍵功能。現在不論您在哪個分頁，只要按下 F1，系統就會自動切換該頁面側邊欄的收合狀態。

## 變更項目

### Form1.vb
- **F1 鍵攔截**：在 `Form1_KeyDown` 中新增 `Keys.F1` 判斷。
- **邏輯抽離**：新增 `PerformSplitterToggle` 方法，將原本分散的縮合/恢復邏輯統一管理，並加入 `_dbg` 記錄方便後續維護。
- **動態分頁支援**：新增 `GetActiveSplitter` 函數，確保 F1 鍵能精準作用於當前顯示的分頁（Tab1 ~ Tab5）。

## 驗證結果
- [x] 在任何焦點位置按下 F1 均可觸發。
- [x] 雙擊分隔線的功能維持正常。
- [x] 縮合至 10px 與恢復原寬度的狀態轉換邏輯正確。
- [x] 跨分頁（Tab1 至 Tab5）切換測試正常。

> [!NOTE]
> 實作中已包含 `SendMessage` 凍結重繪的邏輯，確保在快速切換時 UI 不會產生殘影。
