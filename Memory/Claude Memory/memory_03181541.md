# Outlook Assistant 開發記憶摘要
## 供新 Chat 接手使用 — 產出於 2026-03-18

---

## 專案基本資訊

- **語言/框架**：VB.NET / WinForms
- **環境**：Outlook LTSC 2021（olNotExchange）、本機 PST 檔、VS 2022
- **主要檔案**：Form1.vb（~3422行）、SimTree.vb（474行）、DebugForm.vb、moduleStore.vb
- **5個分頁**：Tab1 資料夾統計、Tab2 依日期統計、Tab3 尋找附件、Tab4 系列郵件（未開發）、Tab5 重複郵件（未完成）

---

## 全域架構原則（已確認，勿再修改）

### COM / 執行緒規則
- 所有 Outlook COM 呼叫必須在 UI 執行緒（STA）
- 不可用 Task.Run 包住 COM 物件
- 用 `Await Task.Yield()` 讓 UI 不凍結，不切換執行緒
- `Task.WhenAll` 只有跨 PST 才真正有效（同一 PST 鎖競爭反而慢）
- **重要**：所有 Catch 要寫 `System.Exception`（因 Imports Outlook 導致命名空間衝突）

### Outlook Namespace 衝突
- `Imports Microsoft.Office.Interop.Outlook` 會讓 `View`、`Exception` 等名稱衝突
- ListView 的 `View` 屬性必須寫完整名稱：`System.Windows.Forms.View.Details`
- Catch 必須寫 `System.Exception` 而非 `Exception`

### Cache 設計（已確定，不改）
```vb
Private Shared ReadOnly mailCountCache As New ConcurrentDictionary(Of Outlook.Folder, Integer)
Private Shared ReadOnly folderCountCache As New ConcurrentDictionary(Of Outlook.Folder, Integer)
Private Shared ReadOnly folderSizeCache As New ConcurrentDictionary(Of Outlook.Folder, Long)
Private Shared ReadOnly folderTreeCache As New ConcurrentDictionary(Of Outlook.Folder, List(Of Outlook.Folder))
Private Shared ReadOnly yearCountsCache As New ConcurrentDictionary(Of String, ConcurrentDictionary(Of Integer, Integer))
Private Shared ReadOnly monthCountsCache As New ConcurrentDictionary(Of String, ConcurrentDictionary(Of Integer, Integer))
' Tab3 Phase1 快取
Private _tab3Phase1Cache As New Dictionary(Of String, Tab3FolderCacheEntry)
```

---

## ESC 全域中斷（完成）

- `Me.KeyPreview = True`（Form1_Load 開頭）
- `Form1_KeyDown` 攔截 ESC → `_cancelRequested = True` + `blnButton3_Stop = True`
- Tab1/Tab2/Tab3 各自在 Yield 點檢查 `_cancelRequested`
- `ShowYearViewAsync` 用 `savedCancel` 暫存旗標

---

## Tab1 資料夾統計（已完成重構 v4）

### 分層架構
```
L1  TreeView1_AfterSelect     UI 事件層：序號防護 + 批次寫 ListView
L2  ComputeFolderStatsAsync   流程協調層：BFS + 快取剪枝 + 底部向上彙總
L3  GetDirectMailCountL3      COM 資料層：只讀 PR_CONTENT_COUNT
```

### 關鍵 Bug（已修正）
1. root 快取命中時仍展開直屬子資料夾（parentIdx=-1 不剪枝）
2. 第二次點選雙重計算（`If Not parent.IsFromCache Then` guard）
3. `GetFolderSizeLINQ` 移除 Task.Run 包 COM（改 GetTable + PR_MESSAGE_SIZE）

### PST Folder Size 結論
- `PR_MESSAGE_SIZE_EXTENDED` 和 `PR_MESSAGE_SIZE` 在本地 PST 均回「未知或找不到」
- `GetTable` 遍歷是唯一可靠方案

---

## Tab2 依日期統計

### 分層架構
```
L1  TreeView2_AfterSelect / SimTree2_AfterSelect
L2  ComputeYearCountsAsync
L3  CountYearsInSingleFolderAsync / CountMonthsInYearAsync
```

### Chart2 月份視圖（Q1 本次修正完成）
```vb
' UpdateChart2ForMonths 的 AxisX 設定（已確認正確）：
With Chart2.ChartAreas(0).AxisX
    .Minimum = 0.5
    .Maximum = 12.5
    .Interval = 1
    .IntervalOffset = 0
    .LabelStyle.Format = ""          ' ← 空字串才能讓 pt.AxisLabel 生效
    .LabelStyle.Interval = 1
    .LabelStyle.IntervalOffset = 0.5
    .MajorTickMark.IntervalOffset = 0
End With
```
- 原本 `LabelStyle.Format = "##"` 會覆蓋 AxisLabel，改成 `""` 後月份標籤正常顯示
- `Minimum = 0.5 / Maximum = 12.5` 讓長條不被截斷並置中

### Chart2 MouseLeave（Q5.1 本次修正完成）
- 將 `Chart2.Refresh()` 移到 `If...End If` **外面**確保一定執行
- 去掉 `Async Sub` / `Await Task.Yield` / `Await Task.Delay`，改回純 `Sub`

### ListView2 ← → Chart2 同步高亮（Q5 待實作）
- `ListView2_SelectedIndexChanged` handler 已存在（行 3409）但 body 是空的，需要填入邏輯
- 年度視圖：用 `CInt(pt.XValue).ToString()` 比對 ListView 選取文字
- 月份視圖：用 `pt.AxisLabel` 比對
- 與 `_lastHoveredPointIndex` 共用（hover 與選取共用同一個高亮狀態）
- 需加 guard: `If Chart2.Series.Count = 0 OrElse Chart2.Series(0).Points.Count = 0 Then Return`

### ListView2_GotFocus（Q3 本次修正完成）
```vb
' 防止 Items 為空時 IndexOutOfRange
If ListView2.SelectedItems.Count = 0 AndAlso ListView2.Items.Count > 0 Then
    ListView2.Items(0).Selected = True
End If
```

### 待解決 Bug A（SimTree2 切 Tab2 焦點跑到最頂）
- **現象**：游標先到收件匣，但很快又跳回最頂
- **根因**：某個路徑在 `ExpandTreeToDefaultInbox` 選好收件匣後清掉了 `_selectedNodes`，
  導致 `SimTree2.Focus()` via BeginInvoke 觸發 `OnGotFocus` 時 `_selectedNodes.Count = 0`，
  進而呼叫 `SelectSingleNode(TopNode)` 跳到最頂
- **注意**：`SimTree.ClearSelectedNodes()` 同時清掉 `_selectedNodes` 和 `_lastClickedNode`，
  但 Form1.vb 裡沒有直接呼叫 `ClearSelectedNodes()` 的地方，確切干擾點未找到
- **建議修正方向**：改 BeginInvoke 的 lambda，在 Focus 前先明確補選收件匣：
```vb
Me.BeginInvoke(Sub()
    ' ★ 防護：若選取被清掉，在 Focus 前補選收件匣，避免 OnGotFocus 跳到 TopNode
    If SimTree2.SelectedNodes.Count = 0 AndAlso SimTree2.Nodes.Count > 0 Then
        Dim inboxNode = SimTree2.Nodes(0).Nodes.Cast(Of TreeNode)().
            FirstOrDefault(Function(n) n.Text.Contains("Inbox") OrElse n.Text.Contains("收件匣"))
        If inboxNode IsNot Nothing Then SimTree2.AddSelectedNode(inboxNode) : inboxNode.EnsureVisible()
    End If
    SimTree2.Focus()
End Sub)
```

---

## Tab3 尋找附件（完成 + 快取）

### 架構（兩階段搜尋）
```
Button3_Click
├── Step3: GetTab3CandidatesFromCacheOrScan() ← Phase1 快取入口
│    └── ScanFolderWithGetTable(_tab3BaseFilter) 無大小篩選全集
├── Step3b: LINQ 大小過濾
├── Step4: 決定是否需要 Phase2
└── Step5: ShowTab3Results()
```

### 已確認死路（勿再嘗試）
- DASL/Restrict 無法篩附件檔名（PST 無索引）
- `AdvancedSearch` 在 LTSC 失敗（HResult=0x8007064F）
- `SetColumns` 加附件屬性 → Attachments=null
- `PR_ATTACH_LONG_FILENAME (0x3707001F)` 在 PST GetTable 拋 NullReference（附件子物件屬性，非 MailItem 頂層）

---

## CacheSniffer（完成）

```vb
Private _cacheSnifferCts As New System.Threading.CancellationTokenSource
' Form1_Load 末尾: CacheSnifferAsync(_cacheSnifferCts.Token)
' FormClosing: _cacheSnifferCts.Cancel()
```
- BFS 廣度優先，每完成一個資料夾等 1 秒
- 依序預熱 Tab1（mailCount + folderCount）、Tab2（yearCounts）、Tab3（_tab3Phase1Cache）

---

## 函數整合計畫（Q4，待實作，先不動）

以下 5 個統一函數將取代目前散落各處的讀取邏輯，每個函數**內部**自行快取（不另外寫 cache_xxx wrapper）：

| 新函數名 | 說明 | 快取 key |
|---|---|---|
| `GetMailCountSingleFolder(folder)` | 單一資料夾郵件數（PR_CONTENT_COUNT + fallback Items.Count），取代 GetDirectMailCountL3 | folderSizeCache? 還是新增 |
| `GetMailCountTotalFolders(folder)` | 含子資料夾遞迴總數，取代 GetMailCountByMAPINew | mailCountCache |
| `GetMailSize(mailItem)` | 單封郵件大小（PR_MESSAGE_SIZE），取代散落的 s.Size | mailSizeCache |
| `GetFolderSizeSingleFolder(folder)` | 單層資料夾大小（GetTable + PR_MESSAGE_SIZE），不含子資料夾 | （新快取） |
| `GetFolderSizeTotalFolders(folder)` | 含子資料夾遞迴總大小，取代 GetFolderSizeLINQ | folderSizeCache |

---

## LookAndFeel 拆分（完成）

```
LookAndFeel()         ← 視窗位置、TabControl、ContextMenu、Chart2、Button
ApplyTreeViewStyles() ← TreeView1-4 / SimTree1-2 字型、顏色、雙緩衝
ApplyListViewStyles() ← ListView1-4 字型、樣式、雙緩衝、欄位定義
```
- 注意：`lv.GridLines = False` 必須明確設定，否則格線出現

---

## Hover 邏輯（HandleTreeViewMouseMoveEvent，完成）

```vb
' 還原 hover：SimTree 選取節點根據 Focused 還原正確色，其他用 Color.Empty
' 套用 hover：SimTree 選取節點 skipHover，其他套淡灰色
```

---

## SimTree 控制項（SimTree.vb，已完成）

- `Shadows SelectedNode`：Set = `SelectSingleNode`（不觸發 AfterSelect）
- `OnGotFocus`：`_selectedNodes.Count = 0` 時自動選 TopNode（這是 Bug A 的間接原因）
- `ClearSelectedNodes()`：同時清 `_selectedNodes` 和 `_lastClickedNode`（Public）
- `AddSelectedNode(node)` — 不清除其他選取，不觸發 AfterSelect
- `_suppressSimTreeAfterSelect`：Form1 成員變數，LoadStoreToTreeView 期間設 True

---

## 可壓縮的 If/Else/End If（Q7，待手動導入）

程式碼中有 3 處符合「5行壓1行」的模式：

**行 2112**（Tab3 Phase2 決策）：
```vb
' 原本 5 行：
If hasKeyword OrElse hasCountFilter Then
    finalItems = Await FilterByAttachDetailAsync(allCandidates)
Else
    finalItems = BuildListItemsFromCandidates(allCandidates)
End If
' 壓成 1 行：
If hasKeyword OrElse hasCountFilter Then finalItems = Await FilterByAttachDetailAsync(allCandidates) Else finalItems = BuildListItemsFromCandidates(allCandidates)
```

**行 2804**（Levenshtein 距離計算）：
```vb
' 壓成 1 行（Else 分支較長，可視情況保留 5 行）：
If strA(i - 1) = strB(j - 1) Then distance(i, j) = distance(i - 1, j - 1) Else distance(i, j) = Math.Min(Math.Min(distance(i - 1, j) + 1, distance(i, j - 1) + 1), distance(i - 1, j - 1) + 1)
```

**行 3108**（首次啟動判斷）：
```vb
' 原本 5 行：
If Not bln1stInit Then
    lblStatus2.Text = "統計花費了 " & sw.Elapsed.TotalSeconds.ToString("0.00") & " 秒。"
Else
    bln1stInit = False
End If
' 壓成 1 行：
If Not bln1stInit Then lblStatus2.Text = "統計花費了 " & sw.Elapsed.TotalSeconds.ToString("0.00") & " 秒。" Else bln1stInit = False
```

---

## 啟動優化（完成）

1. 移除 `GetSortedStores` 裡的 `Marshal.ReleaseComObject(space)`
2. `LoadStoreToTreeView` 無條件加 `":::"` 佔位節點（省掉 Folders.Count COM 往返）

---

## 待辦清單

| 優先 | 項目 | 說明 |
|---|---|---|
| 高 | Tab2 Bug A | SimTree2 切 Tab2 跳到最頂，建議修 BeginInvoke lambda（見上方） |
| 中 | ListView2 ↔ Chart2 同步高亮（Q5） | ListView2_SelectedIndexChanged body 是空的，需填入 |
| 中 | 函數整合（Q4） | GetMailCountSingleFolder / GetFolderSizeTotalFolders 等 5 個統一函數 |
| 中 | Tab3 Phase2 檔名快取 | AttachCandidateInfo 加 List(Of String) 存附件檔名 |
| 低 | Q7 If/Else 壓縮 | 3 處，行 2112 / 2804 / 3108，手動導入 |
| 低 | Tab4 系列郵件 | 尚未開發 |
| 低 | Tab5 重複郵件 | Levenshtein 已實作，UI 未完成 |

---

## 使用者偏好

- ✅ 偏好直接給程式碼，在瀏覽器頁面解釋做法跟原理，在程式碼註解記錄清楚
- ✅ DebugForm clipboard：雙擊=單行，Ctrl+C=多行Tab分隔
- ✅ 詳細中文註解，說明設計意圖與決策理由
- ✅ 每次改動附上日期與原因
- ❌ 不移除 `Parallel.ForEach`（Tab1/Tab2 L3 保留）
- ❌ Tab1 cache key 不改成 FolderPath 字串（接受偶爾 cache miss）
- ⚠️ 不可精簡 namespace（`System.Windows.Forms.View.Details` 不可寫成 `View.Details`）
- ⚠️ 不可亂刪舊有註解，每版輸出前必須確認沒有遺漏
